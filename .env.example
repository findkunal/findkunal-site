# TMDB API key — used by scripts/fetch-posters.mjs to fetch accurate poster images.
# Without this the script falls back to og:image scraping, which is unreliable.
# Free key: https://www.themoviedb.org/settings/api  (sign up → API → Request v3 key)
TMDB_API_KEY=
